apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lead-scoring.fullname" . }}-config
  labels: {{- include "lead-scoring.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
