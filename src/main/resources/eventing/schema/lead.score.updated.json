{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "lead.score.updated",
  "description": "Canonical envelope for the lead.score.updated topic.",
  "type": "object",
  "properties": {
    "eventId": { "type": "string", "format": "uuid" },
    "eventType": { "type": "string", "const": "lead.score.updated" },
    "schemaVersion": { "type": "integer", "minimum": 1 },
    "orgId": { "type": "string" },
    "aggregateType": { "type": "string", "const": "lead" },
    "aggregateId": { "type": "string" },
    "occurredAt": { "type": "string", "format": "date-time" },
    "traceId": { "type": ["string", "null"] },
    "payload": {
      "type": "object",
      "properties": {
        "leadId": { "type": "string" },
        "score": { "type": "number", "minimum": 0, "maximum": 1 },
        "modelVersion": { "type": ["string", "null"] }
      },
      "required": ["leadId", "score"],
      "additionalProperties": true
    }
  },
  "required": ["eventId", "eventType", "schemaVersion", "orgId", "aggregateType", "aggregateId", "occurredAt", "payload"],
  "additionalProperties": true
}
